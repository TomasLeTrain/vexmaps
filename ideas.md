# ideas
* localization model should be initalized by the user
    * this allows the user to specify settings based on the model
* if a class gets added that runs the localization task, it should take a localization model made by the user in
    * this class could also have multiple localization models specified by the user for redundancy
